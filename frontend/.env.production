# Used automatically by `vite build`. Override in CI/Render with VITE_API_URL if needed.
VITE_API_URL=https://cleb-easy-backend-service.onrender.com/api
# Ensure the backend Render service has FRONTEND_URL=https://cleb-easy.onrender.com (see backend/.env.example).

